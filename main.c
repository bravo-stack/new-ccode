#include <stdio.h>

int main() {
    
    // check
    char greeting[] = "Marhaban";
    char food[] = "Jollof Rice";

    printf("%s, ya habibi: \n", greeting);
    printf("I suppose we all fancy a %s\n", food);

    return 0;
}